name: BUILD - Diesel Enum Pg Macro Crate

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build-rust:
    uses: ./.github/workflows/common-pipeline.yml
    secrets: inherit

